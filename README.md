# Effect-TS by Example

Learning Effect-TS through practical examples.

## Getting Started

Install dependencies:
```bash
bun install
```

Run the example:
```bash
bun run example:dev
```

Build the project:
```bash
bun run build
```

## Structure

- `src/example.ts` - Main example file to experiment with Effect-TS
